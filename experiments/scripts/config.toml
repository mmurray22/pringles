###### THIS IS WHERE WE DEFINE EXPERIMENT VARIABLES USED FOR ALL EXPERIMENTS ########

###### Should have all the network information for the components of the system which are run on servers #######
[network_setup]
###### Storage server Network Information #####
# This is the port the storage server(s) will be receiving info on
# There is one receive port per machine, current assumption is each machine has one storage server
stor_recv_port = "30003"
# This is the mac address of every storage server being used
stor_macs = ["42:01:0a:00:00:06", "42:01:0a:00:00:07", "42:01:0a:00:00:08", "42:01:0a:00:00:09", "42:01:0a:00:00:10", "42:01:0a:00:00:11"]
# This is the IP address of every storage server being used
stor_ips = ["10.0.0.6", "10.0.0.7", "10.0.0.8","10.0.0.9", "10.0.0.10", "10.0.0.11"]
# This is the network interface the storage servers use to send/receive
stor_net_ifs = ["ens4", "ens4", "ens4","ens4", "ens4", "ens4"] 
######## Client Network Info #######
# This is the mac address of every client machine
cli_macs = ["42:01:0a:00:00:04"]
# This is the IP address of every client machine
cli_ips = ["10.0.0.4"]
# This is the network interface the client machines use to send/receive
cli_net_ifs = ["ens4"]
####### Switch Network information ########
# NOTE : Only one software switch is supported at this time. I will need to update this TODO
# This is the IP address of the software switch
switch_ip = "10.0.0.5"
# This is the receive port of the software switch
switch_recv_port = "30002"
# This is the network interface the software switch uses to send/receive
switch_net_if = "ens4"
# This is the mac address of every switch machine
switch_mac = "42:01:0a:00:00:05"
###### Authentication Information for the switch #######
# NOTE: I don't know if I need this if ssh key forwarding is used? Might deprecate TODO
# SSH key 
ssh_key = "/home/mathwiz23pi/.ssh/gcloud"
# SSH username
ssh_user = "mathwiz23pi" # Added for complete SSH command

######## Should contain the necessary information to figure out what programs are being run where (and also the setup script?) #######
[program_paths]
# This is the full path to the client binary that will be run on all clients
path_client = "/home/mathwiz23pi/pringles/build/experiments/unit_tests/cli"
# Name of the actual client binary 
client_binary = "cli"
# This is the full path to the storage server binary that will be run on all storage servers
path_server = "/home/mathwiz23pi/pringles/build/experiments/unit_tests/stor"
# Name of the actual storage binary 
server_binary = "stor"
# This is the full path to the software switch binary that will be run on all software switches
path_switch = "/home/mathwiz23pi/pringles/build/experiments/unit_tests/switch"
switch_binary = "switch"
# Kafka binary TODO
# Speclog binary TODO
# Boolean indicating whether the install script should be run to install packages on all machines
# Generally safe to set to "False"
run_setup_script = "False"

######## Defines variables necessary for experiments #######
[experiment_parameters]
# Indicates how much logging information we want printed to the terminal
# Most common log levels to use are "1" for debugging mode (prints everything)
# and "6" (which only prints critical print statements - use for getting actual numbers)
log_level = 6
# How long the system should be sending packets before we collect statistics 
warm_up = 5 # seconds
# How long the system should be sending packets after we collect statistics 
cool_down = 5 # seconds
# How long we should collect statistics for
experiment_duration = 10 # seconds
# Indicates whether we send raw or UDP sockets. Should always be UDP as of now
socket_type = "UDP"
# How large our payloads will be
message_size = 4000 # bytes
# How many clients we will have per client machine
num_client_threads = 1
# How many storage threads there should be handling appends/reads
# NOTE: This is a bit ill-defined at the moment, I would leave at 1 for now TODO
num_storage_threads = 1
# Name of the json file which will hold final results
json_name = "gcp_client"
# Name of the experiment
experiment_name = "increase_shard_size"
# This indicates how many acknowledgements the client will need in order to 
num_failures = 0
# Supposed to be the git hash of the code that we are using for this experiment
git_hash = "1b0aec5ee4e12df008d835842cafe794b9e77615" # TODO
# Boolean indicates whether the current experiments use the switch or not (if not then cli <-> storage servers)
use_switch = 1
# Boolean indicates whetehr the current experiments use the HARDWARE switch or not TODO actually don't use this yet, is it needed?
use_hardware_switch = 0
# Boolean indicates whether the current experiments use the storage server or not (if not then cli <-> switches)
use_store = 1
# Boolean indicates whether the current experiments do sharding or not (if not then cli sends to all storage servers)
use_shard = 1
# Number of packets in a batch
batch_size = 1 # Number of packets
# How long to wait until sending a batch off
batch_usec_timeout = 460
# indices of the switches active in the ring, list of unique values, each val 0 <= v <= MAX_NUM_SWITCHES
switches_in_ring = [2, 3] 
#  I'm going to be honest, I don't quite remember what this was for atm. Just keep it false?
with_tunnel = false
# This is meant to indicate what x variable we want to plot on the final graphs
# Right now it only supports plotting by number of switches in the ring or by number of clients 
#  Boolean indicating that the streaming interface is being used
use_streams = 0
# Shard size
num_servers_per_shard = 2
# Number of shards
num_shards = 1
# Number of acks needed before replying to the client
ack_threshold = 2
# Boolean indicating whether ack counting is done in the switch or the client
use_client_count_acks = 0


# This should really be part of experiment parameters. Also like 3/4 of these are deprecated, I need to update TODO
[protocol_batching]
# Indicates whether batching is being used or not
batch_on = true
# DEPRECATED, ignore these, I need to remove TODO
num_packet_types = 1
sequencer_type = 0 
storage_server_type = 0 

######## This is all information necessary for the actual hardware switches #######
[switches]
# The name if the jump host if we are sending traffic to the switch via the control plane 
jump_host = "watson.millennium.berkeley.edu"
# Jump host username
jump_user = "micahmurray"
# Jump host mac address
jump_mac = "00:25:90:53:e6:00"
# Jump host IP address
jump_ip = "169.229.48.63"
# Path to the tofino model used to simulate the data plane on the software switch. Included, but not used atm 
tofino_model = "/root/bf-sde-9.4.0/run_tofino_model.sh"
# Path to the switch SDE which needs to be deployed to setup the dataplane
switchd = "/root/bf-sde-9.4.0/run_switchd.sh --arch tofino -p sequencing_only"
# Path to the script which runs the test apparatus and also runs the switch control plane
ptf_test = "/root/bf-sde-9.4.0/run_p4_tests.sh --arch tofino -p sequencing_only -t /root/pringles/code/p4/sequencing --veth-exists --target hw"
# Name of the config file which is used by the control plane to setup the switch
config_name = "switch_config.yaml"
# What architecture should be used 
arch = "tofino"
# What p4 program is being run
program = "sequencing_only"
# Absolute path to the control plane which will need to run on the switch CPU
control_plane = "/root/pringles/code/p4/sequencing/test.py"
# This is to indicate whether we are sending dataplane traffic through the switch
use_port_forwarding = true
# IP addresses of all the hardware switches
switch_ips = ["10.229.49.3", "10.229.49.5", "10.229.49.7", "10.229.49.9"]
# MAC addresses of all the hardware switches TODO need to completely fill in
switch_macs = ["00:90:fb:70:65:db", "", "00:90:fb:70:65:d0", "00:90:fb:70:65:71"]
# Front panel ports to be initialized and turned on in the control plane on switch 0 (TODO I wasn't using switch 0, so not filled in rn)
switch_ports0 = [132, 140, 156, 172, 184, 176, 168, 160, 144, 152, 128, 136]
# Front panel ports to be initialized and turned on in the control plane on switch 1
switch_ports1 = [140, 156, 172, 180, 188, 20, 36, 44, 60, 56]
# Front panel ports to be initialized and turned on in the control plane on switch 2
switch_ports2 = [128, 136, 144, 152, 168, 184, 16, 24, 32, 48, 56]
# Front panel ports to be initialized and turned on in the control plane on switch 3
switch_ports3 = [172, 188, 20, 28, 36, 44, 52, 56]
# Front panel port which will be used for loopback traffic 
# TODO need to fill in loopback for switch 0, loopback needs to be on same pipeline as control plane
recirc_ports0 = [188]
recirc_ports1 = [64, 128, 136, 144, 152, 160, 168, 176, 148, 184]
recirc_ports2 = [40, 132, 140, 148, 156, 160, 164, 172, 180, 188]
recirc_ports3 = [168]
# The CPU<->Tofino Data Plane PCIe port to exchange info between control and data plane
cpu_port = 192
#### These are the set of ports that the switches should choose from to send control port out of
### depending on which switches are in the active ring set
# TODO: Need to fill in for switch 0
cntrl_port_switch0 = [188, 148, 164, 180]
cntrl_port_switch1 = [132, 184, 148, 164]
cntrl_port_switch2 = [128, 144, 188, 176]
cntrl_port_switch3 = [132, 148, 164, 168]
# I don't think this is used anymore, but specified in below table entries
dummy_ip_addr = "10.229.49.9"
# This is info used by the control plane to initialize the ipv4 routing table entry
# TODO prob need to update as clients send to more switches
ipv4_table_entries = ["10.229.49.9", "00:25:90:53:e6:00", "192", "169.229.48.63"] # IP to match on
# I don't think this is used anymore, but specified in above table entries
mac_dst_addr = "00:90:fb:70:65:71" # TODO need to add ALL the possible mac addresses here
# I don't think this is used anymore, but specified in above table entries
mac_src_addr = "00:25:90:53:e6:00"
# Numerical ID of the tofino dataplane device on the switch
dev_number = 0
# Interface to send to if sending packets from the control plane to the data plane
cpu_interface = "enp5s0"
# External interface on the switches which can be sent to by the watson machines
external_interface = "ma1"
# Port that the switch CPU is listening to for external traffic 
listen_port = 5005

###### THIS IS WHERE WE DEFINE EXPERIMENTS AND CHANGE EXPERIMENT VARIABLES ########
[[experiment]]
# Vary the number of client threads for this experiment
num_client_threads = 1
# Number of packets in the batch
batch_size = 1
# JSON Name of the specific 
json_name = "shardtwo"
# indices of the switches active in the ring, list of unique values, each val 0 <= v <= MAX_NUM_SWITCHES
switches_in_ring = [3] 
num_servers_per_shard = 2

#[[experiment]]
## Vary the number of client threads for this experiment
#num_client_threads = 1
## Number of packets in the batch
#batch_size = 1
## JSON Name of the specific 
#json_name = "shardthree"
## indices of the switches active in the ring, list of unique values, each val 0 <= v <= MAX_NUM_SWITCHES
#switches_in_ring = [3] 
#num_servers_per_shard = 3
#
#[[experiment]]
## Vary the number of client threads for this experiment
#num_client_threads = 1
## Number of packets in the batch
#batch_size = 1
## JSON Name of the specific 
#json_name = "shardfour"
## indices of the switches active in the ring, list of unique values, each val 0 <= v <= MAX_NUM_SWITCHES
#switches_in_ring = [3] 
#num_servers_per_shard = 4
#
#[[experiment]]
## Vary the number of client threads for this experiment
#num_client_threads = 1
## Number of packets in the batch
#batch_size = 1
## JSON Name of the specific 
#json_name = "shardfive"
## indices of the switches active in the ring, list of unique values, each val 0 <= v <= MAX_NUM_SWITCHES
#switches_in_ring = [3] 
#num_servers_per_shard = 5
#
#[[experiment]]
## Vary the number of client threads for this experiment
#num_client_threads = 1
## Number of packets in the batch
#batch_size = 1
## JSON Name of the specific 
#json_name = "shardsix"
## indices of the switches active in the ring, list of unique values, each val 0 <= v <= MAX_NUM_SWITCHES
#switches_in_ring = [3] 
#num_servers_per_shard = 6


#[[experiment]]
#num_client_threads = 1
#batch_size = 1 # Number of packets
#json_name = "ringtwo"
#switches_in_ring = [2,3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
#[[experiment]]
#num_client_threads = 1
#batch_size = 1 # Number of packets
#json_name = "ringthree"
#switches_in_ring = [1,2,3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
#
#[[experiment]]
#num_client_threads = 2
#batch_size = 1 # Number of packets
#json_name = "two_client"
#switches_in_ring = [3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
#
#[[experiment]]
#num_client_threads = 4
#batch_size = 1 # Number of packets
#json_name = "four_client"
#switches_in_ring = [3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
#
#[[experiment]]
#num_client_threads = 8
#batch_size = 1 # Number of packets
#json_name = "eight_client"
#switches_in_ring = [3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
#
#[[experiment]]
#num_client_threads = 10
#batch_size = 1 # Number of packets
#json_name = "ten_client"
#switches_in_ring = [3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
#[[experiment]]
#num_client_threads = 1
#batch_size = 1 # Number of packets
#json_name = "one_client"
#switches_in_ring = [2, 3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
#[[experiment]]
#num_client_threads = 1
#batch_size = 1 # Number of packets
#json_name = "one_client"
#switches_in_ring = [2, 3] # indices of the switches active in the ring, list of unique values, each val 0 <= v <= 3
##
#[[experiment]]
#num_client_threads = 2
#batch_size = 2 # Numbeyyr of packets
#json_name = "two_client"
#
#[[experiment]]
#num_client_threads = 4
#batch_size = 1 # Number of packets
#json_name = "four_client"

#[[experiment]]
#num_client_threads = 6
#json_name = "six_client"

#[[experiment]]
#num_client_threads = 8
#batch_size = 2 # Number of packets
#json_name = "eight_client"
#
#[[experiment]]
#num_client_threads = 10
#batch_size = 2 # Number of packets
#json_name = "ten_client"

#[[experiment]]
#num_client_threads = 15
#batch_size = 2 # Numbeyyr of packets
#json_name = "fifteen_client"

#[[experiment]]
#num_client_threads = 16
#batch_size = 2 # Number of packets
#json_name = "sixteen_client"

#[[experiment]]
#num_client_threads = 18
#json_name = "eighteen_client"

#[[experiment]]
#num_client_threads = 20
#num_storage_threads = 1
#batch_size = 2 # Number of packets
#json_name = "twozero_client"
#
#[[experiment]]
#num_client_threads = 25
#num_storage_threads = 1
#batch_size = 5 # Number of packets
#json_name = "twofive_client"

#[[experiment]]
#num_client_threads = 30
#num_storage_threads = 1
#batch_size = 6 # Number of packets
#json_name = "thirty_client"
#
#[[experiment]]
#num_client_threads = 35
#num_storage_threads = 1
#batch_size = 7 # Number of packets
#json_name = "threefive_client"
##
#[[experiment]]
#num_client_threads = 40
#num_storage_threads = 1
#batch_size = 7 # Number of packets
#json_name = "forty_client"
####
#[[experiment]]
#num_client_threads = 50
#num_storage_threads = 1
#batch_size = 7 # Numbeyyr of packets
#json_name = "fifty_client"
#
#[[experiment]]
#num_client_threads = 60
#num_storage_threads = 1
#batch_size = 5 # Numbeyyr of packets
#json_name = "sixty_client"
#
#[[experiment]]
#num_client_threads = 70
#num_storage_threads = 1
#batch_size = 8 # Numbeyyr of packets
#json_name = "seventy_client"
#
#[[experiment]]
#num_client_threads = 80
#num_storage_threads = 1
#batch_size = 12 # Numbeyyr of packets
#json_name = "eighty_client"
#
#[[experiment]]
#num_client_threads = 90
#num_storage_threads = 1
#batch_size = 10 # Numbeyyr of packets
#json_name = "ninety_client"
###
#[[experiment]]
#num_client_threads = 100
#num_storage_threads = 1
#batch_size = 12 # Numbeyyr of packets
#json_name = "hundred_client"
#
#[[experiment]]
#num_client_threads = 120
#num_storage_threads = 1
#batch_size = 12 # Numbeyyr of packets
#json_name = "hundredtwenty_client"
#
#[[experiment]]
#num_client_threads = 140
#num_storage_threads = 1
#batch_size = 12 # Numbeyyr of packets
#json_name = "hundredforty_client"
#
#[[experiment]]
#num_client_threads = 150
#num_storage_threads = 1
#batch_size = 12 # Numbeyyr of packets
#json_name = "hundredfifty_client"
#
#[[experiment]]
#num_client_threads = 175
#num_storage_threads = 1
#batch_size = 12 # Numbeyyr of packets
#json_name = "hundredsevenfive_client"
#
#[[experiment]]
#num_client_threads = 200
#num_storage_threads = 1
#batch_size = 15 # Numbeyyr of packets
#json_name = "twohundred_client"

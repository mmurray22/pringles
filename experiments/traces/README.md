# Trace Overview

## Trace Format
Traces are constructed as a double column list, with the first column specifying the type of request and the second column specifying the payload for the request.

### Valid Request Types
append_req
read_req
trim_req
subscribe_req
gettail_req

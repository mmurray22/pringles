#include "network.h"
#include "spdlog/spdlog.h"

void main() {
    /*Default parameters for testing*/

    spdlog::debug("Testing different batch sizes!");
    spdlog::debug("ALL BATCH TESTS PASS!");
    spdlog::debug("Testing different packet types!");
    spdlog::debug("ALL PACKET TYPES TESTS PASS!");
    spdlog::debug("Testing different numbers of IP addresses!");
    spdlog::debug("ALL IP TESTS PASS!");
    spdlog::debug("Testing yaml file processing");
    spdlog::debug("ALL FILE PROCESSING TESTS PASS!");
}
